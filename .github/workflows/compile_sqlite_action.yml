name: build-sqlite-db
run-name: ${{ github.actor }} is compiling the sql file
on: [push]
jobs: 
  compile-sqlite:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: cat FluMutDb5.sql | sqlite3.exe FluMutDb5.sqlite3
